# Repository Structure Audit (Phase 3, Step 1)

Date: 2026-09-09. Read-only. Every "referenced by" cell comes from `grep -rn` over the tracked
tree (hits inside `.venv/`, `node_modules/`, and `.mypy_cache/` ignored). "legacy scripts" means
`scripts/ci/*`, `scripts/migration/*`, `scripts/setup/*`, `scripts/utils/*`,
`scripts/validation/*`: artefacts of an earlier reorganisation that reference each other and
nothing current references them; they are deleted in this same audit, so a path referenced only
by them counts as unreferenced.

Current root: **15 files + 9 tracked dotfiles, 19 directories** (4 of the directories are
ignored build output: `dist/`, `htmlcov/`, `.venv`-style caches, and an untracked `backups/`).

## Root files

| path | what it actually is | referenced by | verdict | reason |
|---|---|---|---|---|
| `README.md` | project front page (rewritten Phase 2) | everything | **keep** | |
| `LICENSE` | MIT | README | **keep** | |
| `CONTRIBUTING.md` | contributor guide | README | **keep** | structure section updated in Step 3 |
| `CODE_OF_CONDUCT.md` | Contributor Covenant | CONTRIBUTING | **keep** | conventional; optional fold into CONTRIBUTING if you want the count lower |
| `CHANGELOG.md` | Keep-a-Changelog, 5 releases, last entry 1.1.0 (2025-01) | CONTRIBUTING | **keep** | add an "Unreleased" entry for Phases 0–3 in Step 3 |
| `Makefile` | 30 targets, several stale (`demo`, `demo-*`, `security` needs bandit/safety, `install-dev` references a `docs` group that does not exist) | README, CONTRIBUTING | **keep** | rewritten in Step 3 with `help` default and the required targets |
| `pyproject.toml` | Poetry + ruff + mypy + pytest config | ci.yml, Dockerfiles, Makefile | **keep** | absorbs `mypy.ini` (see below) |
| `poetry.lock` | lockfile | ci.yml, Dockerfiles | **keep** | |
| `render.yaml` | Render blueprint (free tier, Phase 1) | Render reads it from the root | **keep** | must stay at root |
| `docker-compose.yml` | local dev stack: backend, frontend, redis, postgres, adminer, redis-commander (183 lines) | Makefile, `benchmarks/run_benchmarks.py`, legacy scripts | **delete** (duplicate) | byte-identical to `docker/compose/docker-compose.yml` except `context: .` vs `../..`. Keep the `docker/compose/` copy; `make up` runs it with `-f`. No symlink: compose resolves relative mounts against the file's own directory, so a symlinked copy would break the `../..` context |
| `docker-compose.prod.yml` | full stack: nginx, backend, frontend, redis, postgres, prometheus, grafana (357 lines) | `tests/integration/test_production_readiness.py`, legacy scripts | **delete** (duplicate) | same story; `docker/compose/docker-compose.prod.yml` stays as the full-deployment reference. The test asserts the file exists; it is updated to the new path in the same step |
| `quick_start.sh` | installs, runs a test file that does not exist (`tests/test_basic_working.py`), starts `server/main_simple.py` (not the app) | nothing | **delete** | fold into `make install` + `make dev` |
| `run_all_tests.sh` | runs mypy/black/ruff/pytest with colour output; black is not a gate | `.claude/settings.local.json` only | **delete** | fold into `make check` |
| `mypy.ini` | `python_version=3.11`, `ignore_missing_imports`, three settings at their defaults | mypy reads it in preference to pyproject | **merge into `pyproject.toml`** | `[tool.mypy]` already has `ignore_missing_imports = true`; add `python_version = "3.11"`. Equivalence verified in Step 2 by running mypy before/after (169 files, 0 issues) |
| `coverage.xml` | coverage output | ci.yml (uploads it), `.gitignore` | **delete** (local file only) | ignored; regenerated by `make test` |

## Root dotfiles

| path | what it actually is | referenced by | verdict | reason |
|---|---|---|---|---|
| `.gitignore` | | | **keep** | add `backups/` |
| `.env.example` | env template (rewritten Phase 1) | Dockerfiles (`COPY .env.example`), README | **keep** | |
| `.dockerignore` | build context excludes | docker build (implicit) | **keep** | |
| `.dockerignore.test` | near-duplicate of `.dockerignore` "for test builds"; Docker only ever reads `.dockerignore` | legacy scripts | **delete** | unused by CI, which builds `docker/dockerfiles/Dockerfile` with the default ignore file |
| `.pre-commit-config.yaml` | hooks: pre-commit-hooks, black, isort, ruff 0.1.7, mypy, bandit, commitizen | CONTRIBUTING | **keep** | note: black and isort are not gates (ruff format is); pins are 2023-era. Out of scope here; flagged |
| `.commitlintrc.json` | commitlint rules | nothing (pre-commit uses commitizen, not commitlint; no husky, no package.json at root) | **delete** | |
| `.flake8` | flake8 config | nothing (not in pre-commit, CI, or Makefile; ruff is the linter) | **delete** | no fold needed: nothing runs flake8 |
| `.python-version` | `3.12` for pyenv | `.gitignore` lists it (but it is tracked) | **keep** | harmless; remove from `.gitignore` for consistency |
| `.github/` | `workflows/ci.yml`, `CODEOWNERS`, `RELEASE_TEMPLATE.md` | | **keep** | `RELEASE_TEMPLATE.md` mentions `demo/` (deleted below); update |

## Root directories

| path | what it actually is | referenced by | verdict | reason |
|---|---|---|---|---|
| `src/` | the package | everything | **keep** | |
| `tests/` | unit / integration / contract / e2e / fixtures / load_testing | ci.yml, Makefile, Dockerfile.test | **keep** | |
| `frontend/` | Next.js app | Vercel, compose | **keep** | contains `app/globals.css.backup` (untracked, delete) and a `Dockerfile` whose `COPY nginx/…` lines point at `frontend/nginx/`, which does not exist. Flagged, not fixed here |
| `docs/` | 21 entries | README | **keep** | not part of this step, but note stale material: `PRODUCTION_READINESS_REPORT.md`, `POETRY_SETUP.md`, `VERCEL_CONFIG_NOTES.md`, `guides/README_DEMO.md` (references `config/docker`), `portfolio/`, `deployment/BRANCH_COMPARISON.md`. Suggested follow-up phase |
| `benchmarks/` | harnesses + `results/` (committed artefacts) | ci.yml (`benchmarks/results/`), README, Makefile | **keep** | ci.yml uploads `benchmarks/results/` after a test run that now writes to `benchmarks/results/tmp/`; point the upload at `tmp/` in Step 2. Inside: `test_pinecone_performance.py` and `performance/` are unreferenced; left alone this phase |
| `docker/` | `dockerfiles/` (3), `compose/` (2, canonical), `ignore/` (duplicates of the root dockerignore files) | ci.yml, compose, Makefile | **keep** | `docker/ignore/` → **delete** (duplicates). Gains `nginx/` and `redis/` below |
| `infrastructure/` | `terraform/` (AWS ECS, 4 files), `disaster-recovery/` (`dr_automation.py` + runbook) | nothing | **keep** | full-deployment artefacts; gets a README labelling them "not used by the demo" and absorbs `deploy/` and `monitoring/` |
| `config/` | see next table | legacy scripts, its own README | **delete** (whole tree) | every file is a stale duplicate of something at the root or a pip requirements set superseded by Poetry |
| `deploy/` | Helm chart (`Chart.yaml`, `values.yaml`, 2 templates) + 2 raw k8s manifests | nothing | **move → `infrastructure/kubernetes/`** | real artefacts, but the README no longer advertises Kubernetes; they belong with the other full-deployment infrastructure, labelled |
| `demo/` | `showcase_demo.py` (prints *random* "performance metrics"), `test_api.py`, three empty subdirs | `.github/RELEASE_TEMPLATE.md` | **delete** | fabricated metrics are an interview liability; the live demo replaced it |
| `examples/` | `demo_features.py`: function calling, vision, web search over `aiohttp` (not a dependency) against endpoints that do not exist | nothing | **delete** | |
| `use-cases/` | `customer-support/` template: `README.md`, `system-prompt.txt`, `.env.example` | CONTRIBUTING (one line), old README | **delete** | advertises a "template architecture" the app has no code for (no system-prompt loading from this path). Contents listed here so you can keep the prompt text if you want it |
| `monitoring/` | Prometheus configs (two copies), alert rules, Jaeger config, 3 Grafana dashboards + 2 more under `observability/`, `custom_exporters.py`, an observability compose file | `docker/compose/docker-compose.prod.yml` (prometheus service has no volume mount, so not even that) | **move → `infrastructure/monitoring/`** | the demo runs none of it; label as full-deployment only |
| `nginx/` | `nginx.conf` | `docker/compose/docker-compose.prod.yml` (also expects `nginx/conf.d`, `nginx/ssl`, `nginx/cache`, which do not exist) | **move → `docker/nginx/`** | update the compose mount |
| `redis/` | `redis.conf` | both compose files | **move → `docker/redis/`** | update the compose mounts |
| `scripts/` | see next table | README, Makefile (`bench_demo.py` only) | **keep**, reduced to `bench_demo.py` | |
| `backups/` | two empty `.sql` files from a test run (untracked, not ignored) | nothing | **delete** + add to `.gitignore` | |
| `dist/`, `htmlcov/` | build/coverage output (ignored) | ci.yml uploads `dist/` it builds itself | **delete** locally | `make clean` |

## One level deeper

### `config/`

| path | what it is | referenced by | verdict | reason |
|---|---|---|---|---|
| `config/README.md` | describes `docker/`, `requirements/`, `environments/`, `ci/` subdirs; `ci/` does not exist | nothing | **delete** | |
| `config/docker/compose/docker-compose.yml` (58 lines) | older, smaller dev stack | `docs/guides/README_DEMO.md`, legacy scripts | **delete** | superseded by `docker/compose/docker-compose.yml` |
| `config/docker/compose/docker-compose.demo.yml` | demo stack (repointed in Phase 2) | `scripts/setup/setup_demo.sh` (legacy) | **delete** | same services as the dev compose |
| `config/docker/compose/docker-compose.test.yml` | CI-style stack pulling `ghcr.io/…:latest` | nothing | **delete** | CI uses services, not this |
| `config/docker/compose/docker-compose.load.yml` | k6 + prometheus load stack; mounts `monitoring/prometheus-load.yml`, which does not exist | `benchmarks/run_benchmarks.py` | **delete** | broken; `run_benchmarks.py` gets a note |
| `config/docker/compose/docker-compose.tracing.yml` | Jaeger stack | nothing | **delete** | duplicate of `monitoring/observability/docker-compose.observability.yml` |
| `config/environments/render.yaml` | stale copy of the Render blueprint (`dockerfilePath: ./api/Dockerfile`, a path that has not existed since the `api/` → `src/` move) | legacy scripts | **delete** | |
| `config/environments/.env.example` | stale copy of the root file | legacy scripts | **delete** | |
| `config/requirements/{base,dev,prod}.txt` | pip requirement sets | `tests/load_testing/run_tests.sh`, legacy scripts | **delete** | Poetry is the single source of truth (Phase 2 removed the `src/` copy); `run_tests.sh` message updated |

### `deploy/` → `infrastructure/kubernetes/`

| path | what it is | verdict |
|---|---|---|
| `deploy/helm/chatbot-ai-system/` (`Chart.yaml`, `values.yaml`, `templates/deployment.yaml`, `templates/_helpers.tpl`) | Helm chart | **move → `infrastructure/kubernetes/helm/`** |
| `deploy/templates/{deployment,service}.yaml` | raw manifests | **move → `infrastructure/kubernetes/manifests/`** |

### `docker/`

| path | what it is | verdict |
|---|---|---|
| `docker/dockerfiles/{Dockerfile,Dockerfile.production,Dockerfile.test}` | the three backend images | **keep** |
| `docker/compose/{docker-compose.yml,docker-compose.prod.yml}` | canonical compose files (`context: ../..`) | **keep**; become the only copies |
| `docker/ignore/{.dockerignore,.dockerignore.test}` | duplicates of the root files | **delete** |

### `infrastructure/`

| path | what it is | verdict |
|---|---|---|
| `terraform/{main,ecs,networking,data}.tf` | AWS ECS deployment | **keep**, labelled via new `infrastructure/README.md` |
| `disaster-recovery/dr_automation.py`, `runbooks/critical-service-recovery.md` | DR automation sketch + runbook | **keep**, labelled |

### `monitoring/` → `infrastructure/monitoring/`

| path | what it is | verdict |
|---|---|---|
| `prometheus-config.yaml`, `prometheus/prometheus.yml` | two Prometheus configs | **move** (both; dedupe is a follow-up) |
| `alerting-rules.yaml`, `prometheus/alerts/*.yml` | alert rules | **move** |
| `grafana-dashboards/overview.json`, `grafana/dashboards/*.json`, `observability/grafana-dashboards.json` | dashboards | **move** |
| `jaeger-config.yaml`, `observability/*` | tracing config, SLI/SLO config, custom exporters, observability compose | **move** |

### `scripts/`

| path | what it is | referenced by | verdict |
|---|---|---|---|
| `bench_demo.py` | live-demo benchmark (Phase 2) | README, Makefile | **keep** |
| `audit/SYSTEM_AUDIT_REPORT.md` | the Phase-2 hygiene source document | nothing now | **move → `docs/audits/SYSTEM_AUDIT_REPORT.md`** (kept as history; `docs/REPO_AUDIT.md` joins it there) |
| `__init__.py` | makes `scripts` importable; nothing imports it | nothing | **delete** |
| `dependency_{manager,health_monitor,reporter,visualizer}.py` (2,254 lines) | a dependency-health reporting system that imports each other and nothing else uses | each other | **delete** — listed so you can keep them; they are unreachable from any target, doc, or workflow |
| `backup.sh`, `restore.sh`, `healthcheck.sh` | Docker-era ops scripts | `scripts/setup.sh` (legacy) | **delete** |
| `setup.sh`, `start.sh`, `startup.sh`, `demo.sh`, `test.sh` | overlapping start/run scripts against `main_simple.py` or compose | each other | **delete**; `make dev`/`make up` replace them |
| `validate_project.sh`, `verify_setup.sh`, `production_checklist.sh` | checks for files from the pre-reorg layout (e.g. root `Dockerfile`) | `.claude/settings.local.json`, legacy | **delete** |
| `ci/`, `deployment/`, `docker/`, `migration/`, `setup/`, `utils/`, `validation/` | the earlier reorganisation's own scripts, manifests, and rollback | each other | **delete** |

## Tool config fold

| loose file | reader | pyproject today | action | equivalence |
|---|---|---|---|---|
| `mypy.ini` | mypy 1.17.1 prefers `mypy.ini` over `pyproject.toml` | `[tool.mypy] ignore_missing_imports = true` | add `python_version = "3.11"`, delete `mypy.ini` | the other three keys are mypy defaults; verified by running the gate before and after |
| `.flake8` | nothing | `[tool.ruff]` | delete | no consumer |
| `src/chatbot_ai_system/pytest.ini` | nothing (pytest uses the root `[tool.pytest.ini_options]`) | present | delete | |
| `.coveragerc` | n/a | none | n/a | coverage options stay on the command line in the Makefile |

## Script → Makefile fold (proposed targets, Step 3 writes them)

| target | replaces | command |
|---|---|---|
| `help` (default) | | list targets |
| `install` | `quick_start.sh` steps 1–2 | `poetry install && cd frontend && npm ci` |
| `dev` | `quick_start.sh` step 4, `scripts/start.sh` | `poetry run uvicorn chatbot_ai_system.server.main:app --reload` |
| `dev-frontend` | | `cd frontend && npm run dev` |
| `test` | `run_all_tests.sh` pytest step, `scripts/test.sh` | `poetry run pytest tests/ -q` |
| `test-cov` | | `poetry run pytest tests/ --cov=chatbot_ai_system --cov-report=term --cov-report=xml` |
| `lint` | `run_all_tests.sh` ruff step | `poetry run ruff check .` |
| `format` | | `poetry run ruff format .` |
| `typecheck` | `run_all_tests.sh` mypy step | `poetry run mypy src/ --ignore-missing-imports` |
| `check` | `run_all_tests.sh` | `lint typecheck test` |
| `build-frontend` | | `cd frontend && npm run build` |
| `up` / `down` / `logs` | `compose-*`, `demo`, `demo-clean`, `scripts/startup.sh` | `docker compose -f docker/compose/docker-compose.yml up -d` / `down` / `logs -f` |
| `up-full` | `docker-compose.prod.yml` | `docker compose -f docker/compose/docker-compose.prod.yml up -d` |
| `docker-build` | | `docker build -f docker/dockerfiles/Dockerfile -t chatbot-ai-system:dev .` |
| `bench` | | `poetry run python scripts/bench_demo.py` |
| `evidence` | (existing) | `BENCHMARK_RESULTS_DIR=benchmarks/results poetry run pytest tests/test_provider_failover.py` |
| `clean` | | remove caches, `dist/`, `htmlcov/`, `coverage.xml`, `backups/` |

Dropped from the current Makefile: `security` (bandit/safety are not installed), `install-dev`
(`docs` group does not exist), `demo`, `demo-benchmark`, `demo-test`, `demo-clean`, `ci`,
`ci-local`, `pre-commit`, `version`, `lock`, `update` (use Poetry directly).

## References that change in Step 2 (so nothing dangles)

- `.github/workflows/ci.yml`: benchmark upload path `benchmarks/results/` → `benchmarks/results/tmp/`, or set `BENCHMARK_RESULTS_DIR`. Every other CI path (`docker/dockerfiles/Dockerfile`, `src/`, `tests/unit`, `tests/test_provider_failover.py`, `pyproject.toml`, `poetry.lock`) is untouched.
- `Makefile`: rewritten (Step 3).
- `docker/compose/docker-compose.yml` and `docker-compose.prod.yml`: `./redis/redis.conf` → `./docker/redis/redis.conf`, `./nginx/…` → `./docker/nginx/…` (paths are relative to the project directory `../..`); the `./scripts/init-db.sql` and `./scripts/backup` mounts point at files that do not exist today and are removed.
- `tests/integration/test_production_readiness.py::test_docker_compose_files_exist`: new paths.
- `benchmarks/run_benchmarks.py`, `tests/load_testing/run_tests.sh`: references to `config/docker/compose/docker-compose.load.yml` and `config/requirements/base.txt` replaced with a clear "not available" message.
- `.github/RELEASE_TEMPLATE.md`, `CONTRIBUTING.md`, `docs/guides/README_DEMO.md`: mentions of `demo/`, `use-cases/`, `config/docker`.
- `.gitignore`: add `backups/`; stop ignoring `.python-version` since it is tracked.
- New: `infrastructure/README.md` (what is in there and that the demo uses none of it).

Not changed, flagged for a later pass: `frontend/Dockerfile` copies `nginx/nginx.conf` and
`nginx/default.conf` from the frontend build context where no such files exist (the image
cannot currently build); `.pre-commit-config.yaml` pins 2023 tool versions and runs black/isort
which are not gates; `docs/` has six stale documents; `benchmarks/` has unreferenced
`test_pinecone_performance.py` and `performance/`.

## Proposed final root

Files (9 visible + 5 dotfiles = 14):

```
README.md  LICENSE  CONTRIBUTING.md  CODE_OF_CONDUCT.md  CHANGELOG.md
Makefile  pyproject.toml  poetry.lock  render.yaml
.env.example  .gitignore  .dockerignore  .pre-commit-config.yaml  .python-version
```

Directories (8 + `.github`):

```
src/  tests/  frontend/  docs/  docker/  infrastructure/  benchmarks/  scripts/  .github/
```

| target | result |
|---|---|
| ≤ 12 root files | 9 visible files: **met**. 14 counting dotfiles: two over. The five dotfiles are all conventional and consumed by tools (`git`, `docker`, `pre-commit`, `pyenv`, `cp .env.example .env`). To reach 12 including dotfiles: fold `CODE_OF_CONDUCT.md` into `CONTRIBUTING.md` and drop `.python-version` (pyenv only; `pyproject` already states `requires-python`). Your call. |
| ≤ 9 root directories | 8 visible + `.github`: **met** |

From 15 + 9 dotfiles / 19 directories to 9 + 5 / 8. Nothing is moved until you approve or
amend the verdicts above.
