# Lightning CSS requires platform-specific binaries via optionalDependencies
# These install by default with npm ci unless explicitly omitted
# The invalid config "optional=true" has been removed (not a valid npm option)

# Use production registry
registry=https://registry.npmjs.org/

# Performance: disable audit/fund during CI builds
audit=false
fund=false
