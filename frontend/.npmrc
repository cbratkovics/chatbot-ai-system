# Ensure optional dependencies are installed
# Required for platform-specific binaries like Lightning CSS
optional=true

# Disable package-lock.json modification warnings
save-exact=false

# Use production registry
registry=https://registry.npmjs.org/

# Disable audit during CI builds (performance)
audit=false
fund=false
