apiVersion: v1
kind: Service
metadata:
  name: ai-chatbot-api
  labels:
    app: ai-chatbot
    component: api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ai-chatbot
    component: api